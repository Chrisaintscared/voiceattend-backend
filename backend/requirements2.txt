# ============================================================
# VoiceAttend AI – Python Backend Requirements
# ============================================================
# Install inside a virtual environment:
#   python -m venv venv
#   venv\Scripts\activate          (Windows)
#   source venv/bin/activate        (macOS / Linux)
#   pip install -r requirements.txt
# ============================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9       # required for UploadFile / form-data

# --- Database ---
psycopg2-binary==2.9.9        # PostgreSQL adapter (binary wheel, no build tools needed)

# --- Audio Processing ---
librosa==0.10.2
soundfile==0.12.1             # librosa backend for WAV I/O
numpy==1.26.4

# --- Deep Learning (choose ONE – comment out the other) ---
# PyTorch (CPU-only; swap for GPU wheel from https://pytorch.org/get-started/locally/)
torch==2.3.0
torchaudio==2.3.0

# TensorFlow (alternative)
# tensorflow==2.16.1

# --- Utilities ---
pydantic==2.7.1
python-dotenv==1.0.1          # for .env config files
httpx==0.27.0                 # async HTTP client (useful for testing)
